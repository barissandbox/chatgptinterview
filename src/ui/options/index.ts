/** Options-page entrypoint. */

const message = document.getElementById('optionsMessage');
if (message) {
  message.textContent = 'Open ChatGPT Interview from the toolbar on any supported web page.';
}
